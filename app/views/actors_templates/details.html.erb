<h1>Actor #<%= @the_id %> details</h1>
<a href="/actors">Go back</a>

<hr>

<dl>

  <dt>Name</dt>
    <dd> <%= @the_actor.name %></dd>
  <dt>Dob</dt>
    <dd> <%= @the_actor.dob %></dd>
  <dt>Bio</dt>
    <dd> <%= @the_actor.bio %></dd>
  <dt>Image</dt>
    <dd><img src="<%= @the_actor.image %>" width="200"></dd>
  <dt>Created at</dt>
  <dd> <%= @the_actor.created_at %></dd>
  <dt>Updated at</dt>
  <dd> <%= @the_actor.updated_at %></dd>
  
</dl>

<hr>

<h2>Filmography</h2>
<table border="1">

<tr>
<th>Title</th>
<th>Year</th>
<th>Director</th>
<th>Character Name</th>
<th></th>
</tr>

<% character_roles = Character.where({ :actor_id => @the_actor.id })%>
<% character_roles.each do | character | %>
<tr>
  <td>
      <% matching_records = Movie.where({ :id => character.movie_id })%>
      <% movie = matching_records.at(0) %>
      <%= movie.title %>
   </td>
  <td> <%= movie.year %> </td>
  <td> 
      <% matching_records = Movie.where({ :id => character.movie_id })%>
      <% movie = matching_records.at(0) %>
      <% matching_director = Director.where({ :id => movie.director_id })%>
      <% director = matching_director.at(0) %>
      <%= director.name %>
  </td>
  <td> <%= character.name %> </td>
  <td><a href="/movies/<%= movie.id %>">Show details</a></td>
</tr>

<% end %>

</table>
